import { useEffect, useState } from "react";
import data from "./data/node_frequency.json"

const Show = () => {
    

    // console.log("node", nodes)

//   useEffect(() => {
//     {Object.entries(data).forEach(([key, value], i) => (
//       console.log("data", key),
//       console.log("index", i),
  
//       setNodes({id: i, value: value, label: key })
//       <li key={key}>{key}: {value}</li>
//     ))}
//     setNodes({id: 1, value: 3, label: "Hey"})
//   }, [])
}

export default Show